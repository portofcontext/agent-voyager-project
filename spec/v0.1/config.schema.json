{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aep.dev/schema/v0.1/config.schema.json",
  "title": "AEP Config (v0.1)",
  "description": "Entry-point schema for the supervisor → runner Config. Validates the single JSON document the supervisor writes to the runner's stdin (or POSTs to the runner's HTTP endpoint) at startup.",
  "$ref": "https://aep.dev/schema/v0.1/aep.schema.json#/$defs/Config"
}
